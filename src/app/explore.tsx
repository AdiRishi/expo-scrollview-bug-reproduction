import { Text } from "react-native";
import { StandardScrollView } from "@/components/standard-scroll-view";


export default function Explore() {
  return (
    <StandardScrollView>
      <Text>Explore (ScrollView with useSafeAreaInsets)</Text>
    </StandardScrollView>
  );
}
